const _table_name = {
    data_neuron_images: "data_neuron_images",
    lang_preference: "lang_preference"
}

const _status = {
    Active: "active",
    Inactive: "inactive"
}

module.exports = {
    _table_name,
    _status
}